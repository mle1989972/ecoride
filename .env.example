# ==== EcoRide - Exemple de configuration (.env) ====

# Base de données (PostgreSQL)
DATABASE_URL=postgres://user:pass@localhost:5432/ecoride

# JWT pour l'authentification
JWT_SECRET=change-me

# SMTP (optionnel - recommandé en dev : Mailhog)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM="EcoRide <no-reply@ecoride.local>"

# NoSQL (optionnel - logs)
MONGODB_URI=mongodb://localhost:27017/ecoride
